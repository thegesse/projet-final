file structure and plans


backend/
    java srping
front end/ (reminder to change this to mobile frontend)
    dart mobile app
pc front end/
    thymleaf or JS + html css

db org
    tables: user, songs, playlists
    private user + playlists
    public: songs

connections: 
        user -> playlist(by default has liked playlist) -> songs


features:
    login and playlist saving to db, searchup function, add to playlist, player, CRUD operations on the dashboard, optionnal: artist option = release songs -> reviewed by moderation


